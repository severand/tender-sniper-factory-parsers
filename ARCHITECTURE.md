# 🏗️ Factory Parsers - Подробная Архитектура

## Оглавление

1. [Объективы](#%D0%BE%D0%B1%D1%8A%D0%B5%D0%BA%D1%82%D0%B8%D0%B2%D1%8B)
2. [Общая архитектура](#%D0%BE%D0%B1%D1%89%D0%B0%D1%8F-%D0%B0%D1%80%D1%85%D0%B8%D1%82%D0%B5%D0%BA%D1%82%D1%83%D1%80%D0%B0)
3. [Компоненты](#%D0%BA%D0%BE%D0%BC%D0%BF%D0%BE%D0%BD%D0%B5%D0%BD%D1%82%D1%8B)
4. [Поток данных](#%D0%BF%D0%BE%D1%82%D0%BE%D0%BA-%D0%B4%D0%B0%D0%BD%D0%BD%D1%8B%D1%85)
5. [Взаимодействие компонентов](#%D0%B2%D0%B7%D0%B0%D0%B8%D0%BC%D0%BE%D0%B4%D0%B5%D0%B9%D1%81%D1%82%D0%B2%D0%B8%D0%B5-%D0%BA%D0%BE%D0%BC%D0%BF%D0%BE%D0%BD%D0%B5%D0%BD%D1%82%D0%BE%D0%B2)
6. [Технологии](#%D1%82%D0%B5%D1%85%D0%BD%D0%BE%D0%BB%D0%BE%D0%B3%D0%B8%D0%B8)
7. [Модели данных](#%D0%BC%D0%BE%D0%B4%D0%B5%D0%BB%D0%B8-%D0%B4%D0%B0%D0%BD%D0%BD%D1%8B%D1%85)
8. [Обработка ошибок](#%D0%BE%D0%B1%D1%80%D0%B0%D0%B1%D0%BE%D1%82%D0%BA%D0%B0-%D0%BE%D1%88%D0%B8%D0%B1%D0%BE%D0%BA)
9. [Мониторинг и логирование](#%D0%BC%D0%BE%D0%BD%D0%B8%D1%82%D0%BE%D1%80%D0%B8%D0%BD%D0%B3-%D0%B8-%D0%BB%D0%BE%D0%B3%D0%B8%D1%80%D0%BE%D0%B2%D0%B0%D0%BD%D0%B8%D0%B5)

---

## Объективы

### Первичные цели

1. **Масштабируемость** — Обрабатывать 100k+ тендеров в день
2. **Надежность** — Каждые данные должны быть обработаны гарантированно
3. **Отказоустойчивость** — Грациозная деградация при сбоях
4. **Легкость настройки** — Добавлять новые площадки без кода
5. **Прозрачность** — Все модули и операции простым и очевидными

---

## Общая архитектура

### Модульная структура

```
factory_parsers/
├── admin_service/             → Определение площадок и рулявского
├── scheduler_service/         → Планирование задач
├── web_scraper_service/       → Web-парсинг (Scrapy+Playwright)
├── api_scraper_service/       → Официальные API
├── file_fetcher_service/      → Скачивание файлов (PDF, DOCX, XLSX)
├── doc_extractor_service/     → Парсинг содержимого файлов
├── ai_extractor_service/      → ЛМ-структурирование текста
├── normalizer_service/        → Нормализация данных
├── anti_bot_layer/            → Защита от антиботов и капч
├── cache_layer/               → Redis кэш
├── storage_layer/             → PostgreSQL и Elasticsearch
├── client_api_service/        → API для клиента
└── shared/                    → Общие утилиты
```

---

## Компоненты

### 1. `admin_service` — Определение площадок

**Назначение**: Это сердце системы конфигурации.

**Основные сущности** (в PostgreSQL):

| Сущность | Описание |
|---|---|
| `Platform` | Тендерная площадка (e-tender.kz, zakupki.gov.ru и т.д.) |
| `SearchRule` | Правила поиска для каждой площадки |
| `FieldMapping` | Маппинг полей к единому формату |

**Платформа (пример)**:
```python
{
    "id": "uuid",
    "name": "e-tender.kz",
    "base_url": "https://www.etender.kz",
    "has_official_api": true,
    "api_url": "https://api.etender.kz/v1",
    "api_auth_type": "bearer",  # bearer, basic, key, custom
    "mode": "auto",  # auto, api_only, web_only
    "rate_limit_rps": 5,
    "proxy_profile": "mobile",  # mobile, desktop, headless
    "captcha_profile": "standard",
    "js_required": false,
    "is_active": true,
    "created_at": "2025-12-16",
    "updated_at": "2025-12-16"
}
```

**Доступ**: FastAPI routes `/api/admin/platforms/`, `/api/admin/rules/`

---

### 2. `scheduler_service` — Планирование задач

**Назначение**: По расставленным расписаниям (cron) делит труд на тысячи маленьких задач.

**Основные задачи**:
- `FetchTendersAPI(platform_id, rule_id, date_range)` → очередь
- `FetchTendersWeb(platform_id, rule_id, page)` → очередь
- `FetchFiles(tender_id)` → очередь

**Контроль рассытки**:
- учитываются `rate_limit_rps` для каждой площадки
- глобальный лимит на очередь

**Технологии**: APScheduler или Celery Beat + Redis/RabbitMQ

---

### 3. `web_scraper_service` — Scrapy + Playwright

**Назначение**: Вторичный способ сборки данных для площадок без API или неработающим API.

**Феачеры**:
- **Scrapy** — основной фреймворк
- **Playwright** — для JS-сайтов (`js_required=true`)
- **Middlewares** — прокси, HTTP кэш, retry логика, User-Agent ротация
- **Pipelines** — отправка сразу в normalizer_service

**Где живет код**:
- `static/` — статические спайдеры для 4 текущих площадок
- `dynamic/` — генерируются из конфига admin_service

---

### 4. `api_scraper_service` — Официальные API

**Назначение**: Первый ансамбль — используем ресты API на площадках до достижения rate_limit.

**Архитектура**:
```python
api_scraper_service/
├── base_client.py       # Абстрактный класс
├── clients/
│   ├── etender_client.py
│   ├── zakupki_client.py
│   └── ...
└── worker.py            # Celery таски
```

**Каждый клиент**:
- подставляет токены/ключи
- особлюдает лимиты
- возвращает "сырой" тендер в унифицированном виде

---

### 5. `file_fetcher_service` — Скачивание файлов

**Назначение**: Ответственная загрузка PDF/DOCX/XLSX/ZIP и кэширование.

**Процесс**:
1. Получить URL файла
2. Проверить кэш по `file_hash`
3. Если кэш проохов, скачать
4. Схоранить в S3/MinIO
5. Написать в таблицу `files`
6. Опубликовать задачу `ExtractText` в очередь

---

### 6. `doc_extractor_service` — Парсинг содержимого

**Назначение**: вытаскиваем текст и таблицы.

**Поддерживаемые форматы**:
- PDF — pypdf2, pdfplumber, pdfminer
- DOCX — python-docx
- XLSX — openpyxl
- ZIP — рекурсивно расставляем

**Выход**: `DocumentText` (кто нто читать) → очередь `ExtractAI`

---

### 7. `ai_extractor_service` — ЛМ-структурирование

**Назначение**: Пропускали текст через LLM для экстракции структурированных данных.

**ЛМ-клиент**:
```python
llm_client.py
├── OpenAI (GPT-4)
├── Anthropic (Claude)
├── Ollama (Self-hosted)
└── Local LLaMA
```

**Фасы юрите**:

```yaml
prompts/
├── tender_struct.md      → Вытаски сумма, место, инфооабраждвание
└── tender_summary.md     → Текстовое резюме для клиента
```

**Обработка больших документов**:
- Разбиваются на чанки (512-1024 токенов)
- Объединяются результаты

---

### 8. `normalizer_service` — Нормализация

**Назначение**: собираем все данные (от API/Web + AI-выводы + данные файлов) в единый объект.

**Процесс**:
1. Получить "сырой" тендер и вывод AI
2. Запросить `FieldMapping` для площадки
3. Маппировать и агрегировать
4. Вогдить в PostgreSQL (таблица `tenders_normalized`)
5. Ндексировать в Elasticsearch

---

### 9. `anti_bot_layer` — Встроенная защита

**Назначение**: ередия набиринг для детекции и решения антиботов.

**Основные компоненты**:

| Компонент | Описание |
|---|---|
| `proxy_manager.py` | ротация прокси для избежания блокировки |
| `fingerprint.py` | User-Agent, заголовки, TLS |
| `captcha/solver_client.py` | интеграция 2Captcha/CapSolver |
| `captcha/recaptcha.py` | reCAPTCHA v2/v3 |
| `captcha/hcaptcha.py` | hCaptcha |
| `captcha/turnstile.py` | Cloudflare Turnstile |
| `detectors/cloudflare_detector.py` | автодетекция Cloudflare |
| `detectors/captcha_detector.py` | автодетекция капч |

**Флоу решения капчи**:
```
Новый запрос
    ↓
Отправка с ротируемыми прокси
    ↓
4ет1 = Ок?
    ↓ (nope)
5xx? 403/429?
    ↓ (yes)
Running Cloudflare detector
    ↓
Turnstile detected?
    ↓ (yes)
Playwright + solver_client
    ↓
Try again with Playwright
```

---

## Поток данных

```
1. admin_service: Определи площадки
              ↓
2. scheduler: По расписанию создай таски
              ↓
3. Партыэлись:
   ├── API Scraper → Raw Tenders
   └── Web Scraper (Playwright) → Raw Tenders
              ↓
4. File Fetcher: Загружай и кэшируй файлы
              ↓
5. Doc Extractor: Ех парсинг
              ↓
6. AI Extractor: Насыщай через LLM
              ↓
7. Normalizer: Чисти равработка
              ↓
8. PostgreSQL + Elasticsearch: Конечная данные
              ↓
9. Client API: Отдач клиентам
```

---

## Взаимодействие компонентов

### Комуникация между сервисами

- **Synchronous** (запосы в баду):
  - admin_service → storage_layer (get Platform config)
  - normalizer_service → storage_layer (write Tender)
  
- **Asynchronous** (через Celery/RabbitMQ):
  - scheduler → api_scraper, web_scraper (queue tasks)
  - api_scraper/web_scraper → file_fetcher (queue files)
  - file_fetcher → doc_extractor (queue extraction)
  - doc_extractor → ai_extractor (queue AI)
  - ai_extractor → normalizer (queue normalization)

---

## Технологии

| Лайер | Технология | Примеры |
|---|---|---|
| **Parsing** | Scrapy, Playwright | web_scraper_service, anti_bot_layer |
| **Queue** | Celery, Redis, RabbitMQ | scheduler, workers |
| **DB** | PostgreSQL, SQLAlchemy | storage_layer, admin_service |
| **Search** | Elasticsearch/OpenSearch | indexing, search |
| **Cache** | Redis, Scrapy HTTPCache | cache_layer, file_cache |
| **Storage** | S3, MinIO | file_fetcher_service |
| **LLM** | OpenAI, Claude, Ollama | ai_extractor_service |
| **Captcha Solving** | 2Captcha, CapSolver | anti_bot_layer |
| **API** | FastAPI | admin_service, client_api_service |
| **Testing** | pytest, unittest | tests/ нирвсдю сервисе |
| **Logging** | Python logging, structlog | shared/logger.py |
| **Monitoring** | Prometheus, OpenTelemetry | shared/ |
| **Deployment** | Docker, docker-compose, Kubernetes | docker/ |

---

## Модели данных

### Тендер (нормализированный)

```json
{
  "id": "uuid",
  "platform_id": "uuid",
  "external_id": "string",
  "title": "string",
  "description": "string",
  "summary": "string",
  "budget": {
    "amount": 1000000,
    "currency": "KZT"
  },
  "dates": {
    "published": "2025-12-16",
    "deadline": "2025-12-31",
    "start": "2026-01-01",
    "end": "2026-12-31"
  },
  "customer": {
    "name": "string",
    "contact": "string"
  },
  "requirements": [],
  "criteria": [],
  "restrictions": [],
  "ai_extracted": {
    "key_points": [],
    "risk_flags": []
  },
  "files": [
    {
      "id": "uuid",
      "url": "string",
      "filename": "string",
      "content_type": "string",
      "size_bytes": 1024,
      "s3_path": "string"
    }
  ],
  "metadata": {
    "raw_data_id": "uuid",
    "created_at": "2025-12-16T12:00:00Z",
    "updated_at": "2025-12-16T12:00:00Z",
    "processing_time_ms": 5000
  }
}
```

---

## Обработка ошибок

### Стратегия Retry

- **Воркер-задачи**: 3 попытки с exponential backoff (1s, 4s, 16s)
- **HTTP-сонз (в Scrapy)**: 429 → дочти (ОК), если 5xx → ритрай
- **Капчи**: при неудаче режым дожидания 60с + retry

### Кустомные исключения

```python
shared/exceptions.py:
- PlatformNotFound
- ParseError
- FileFetchError
- CapchaSolveError
- AIExtractionError
- NormalizationError
```

---

## Мониторинг и логирование

### Метрики каждого модуля

```
api_scraper:
  - всего_тендеров
  - успешных
  - ошибки (by type)
  - время_ответа_api

web_scraper:
  - всего_паж
  - блокировки (статус 403/429)
  - капчи (всего, эреров)
  - время_ресндеринг

file_fetcher:
  - всего_файлов
  - сгачанных
  - из кэша
  - размер_данных

ai_extractor:
  - документов_обработанных
  - токенов_использовано
  - стоимость_api
```

### Логи

все в JSON-формате (structlog):

```json
{
  "timestamp": "2025-12-16T12:00:00Z",
  "service": "web_scraper",
  "level": "INFO",
  "message": "Tender parsed",
  "tender_id": "uuid",
  "platform": "e-tender.kz",
  "duration_ms": 1500,
  "status": "success"
}
```

---

## Заключение

Эта архитектура делает систему:

✅ **Масштабируемой** — любые компоненты могут запускаться отдельно  
✅ **Надежной** — все таски idempotent, retry логика всюду  
✅ **Легко расширяемой** — добавлять новые площадки деньгамы конфига  
✅ **Прозрачной** — все данные наследительные, нет тайных операций
